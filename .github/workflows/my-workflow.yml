name: My Workflow

on:
  workflow_dispatch:

jobs:
  run-dry-run:
    runs-on: ubuntu-latest
    steps:
      - name: Install Requirements
        run: ls- la

      - name: Install Requirements
        run: pip install -r requirements.txt

      - name: Execute Dry Run
        run: robot --dryrun web/tests/FailedDryRun.robot

